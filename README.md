# Elastic Labs Landing Page

This repository contains the static landing page for Elastic Labs, a game and mobile app development studio. The site is built with plain HTML, CSS, and JavaScript so it can be hosted directly on GitHub Pages without additional tooling.

## Local preview

Open `index.html` in your browser to preview the site locally. All assets are loaded from the same directory or CDNs, so no build step is required.
